debut
    entier nombre;
    
    lire nombre;
    
    si (nombre > 0) alors
        ecrire 1;
    sinon
        si (nombre < 0) alors
            ecrire 0;
        sinon
            ecrire 2;
        finsi
    finsi
fin
