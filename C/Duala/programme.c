#include <stdio.h>
#include <stdlib.h>

int main() {
    int a = 0;  // Variable déclarée
    int b = 0;  // Variable déclarée
    int resultat = 0;  // Variable déclarée
    printf("Tɔlɛ valeur na a: ");
    scanf("%d", &a);
    printf("Tɔlɛ valeur na b: ");
    scanf("%d", &b);
    resultat = (a / b);
    printf("%d\n", resultat);
    printf("\n--- Suka programme ---\n");
    return 0;
}
